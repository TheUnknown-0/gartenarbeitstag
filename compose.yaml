services:
  db:
    image: mariadb:11.4
    container_name: gartenarbeitstag-db
    # Ohne Restart-Policy startet die Datenbank nach einem Host-Neustart nicht
    # wieder mit — die App liefe dann ins Leere.
    restart: unless-stopped
    environment:
      TZ: Europe/Berlin
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASS}
      MYSQL_DATABASE: ${DB_NAME:-gartenarbeitstag}
      MYSQL_USER: ${DB_USER:-gartenarbeitstag}
      MYSQL_PASSWORD: ${DB_PASS}
    volumes:
      - db_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  app:
    build: .
    container_name: gartenarbeitstag-app
    ports:
      - "${APP_PORT:-9020}:80"
    environment:
      TZ: Europe/Berlin
      DB_HOST: db
      DB_USER: ${DB_USER:-gartenarbeitstag}
      DB_PASS: ${DB_PASS}
      DB_NAME: ${DB_NAME:-gartenarbeitstag}
      APP_ENV: ${APP_ENV:-production}
      BASE_URL: ${BASE_URL:-/}
      # Wie lange der Start auf die Datenbank wartet, bevor er aufgibt.
      DB_WAIT_TIMEOUT: ${DB_WAIT_TIMEOUT:-120}
      # Nur diese Adressen dürfen X-Forwarded-For setzen. Leer = niemand.
      TRUSTED_PROXIES: ${TRUSTED_PROXIES:-}
      # E-Mail-Versand (Erinnerungen, Test-Mail). Leer = Versand deaktiviert.
      MAIL_HOST: ${MAIL_HOST:-}
      MAIL_PORT: ${MAIL_PORT:-587}
      MAIL_ENCRYPTION: ${MAIL_ENCRYPTION:-tls}
      MAIL_USER: ${MAIL_USER:-}
      MAIL_PASS: ${MAIL_PASS:-}
      MAIL_FROM_ADDRESS: ${MAIL_FROM_ADDRESS:-}
      MAIL_FROM_NAME: ${MAIL_FROM_NAME:-Gartenarbeitstag}
    volumes:
      - uploads:/var/www/html/uploads
    # "default" muss ausdruecklich dabeistehen: sobald ein Service eigene
    # Netze nennt, faellt das implizite Standardnetz weg — die App erreichte
    # die Datenbank dann nicht mehr.
    networks:
      - default
      - proxy_network
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    # Ohne eigenen Healthcheck bleibt der Zustand „Container läuft, Anwendung
    # antwortet nicht“ von außen unsichtbar.
    healthcheck:
      test: ["CMD", "php", "-r", "exit(@file_get_contents('http://127.0.0.1/healthz') === 'ok' ? 0 : 1);"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 40s
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  phpmyadmin:
    image: phpmyadmin:latest
    container_name: gartenarbeitstag-phpmyadmin
    profiles: ["tools"]
    restart: unless-stopped
    ports:
      - "${PMA_PORT:-8091}:80"
    environment:
      PMA_HOST: db
    depends_on:
      - db

networks:
  # Wird vom Reverse-Proxy bereitgestellt und liegt ausserhalb dieses
  # Projekts — Compose legt es nicht an, es muss vorher existieren.
  proxy_network:
    external: true

volumes:
  db_data:
  uploads:
